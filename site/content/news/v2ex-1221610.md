+++
title = "深度解读：MIMO 测试我的生图 Skill，随口一句测试要求生成“极简图片”，居然生成的是日本国旗。。。"
description = "消息称，大晚上我手搓了一个 skill 。skill 很简单，调用 evolink 的 api 来生成图片。。 然后我让 Agent 调用这个 skill 来生成一个测试图片，测试一下 skill 里面的脚本。"
seo_title = "深度解读：MIMO 测试我的生图 Skill，随口一句测试要求生成“极简图片”，居然生成的是日本国旗。。。｜AI资讯解读 - AI热榜"
seo_description = "消息称，大晚上我手搓了一个 skill 。skill 很简单，调用 evolink 的 api 来生成图片。。 然后我让 Agent 调用这个 skill 来生成一个测试图片，测试一下 skill 里面的脚本。"
seo_keywords = "深度解读：MIMO 测试我的生图 Skill，随口一句测试要求生成“极简图片”，居然生成的是日本国旗。。。, V2EX, AI新闻, AI资讯, AI热榜"
slug = "v2ex-1221610"
type = "news"

[params]
id = "v2ex-1221610"
name = "深度解读：MIMO 测试我的生图 Skill，随口一句测试要求生成“极简图片”，居然生成的是日本国旗。。。"
title_en = "深度解读：MIMO 测试我的生图 Skill，随口一句测试要求生成“极简图片”，居然生成的是日本国旗。。。"
original_url = "https://www.v2ex.com/t/1221610"
source = "V2EX"
published = "2026-06-20T02:56:36"
lang = "zh"
intro = "消息称，大晚上我手搓了一个 skill 。skill 很简单，调用 evolink 的 api 来生成图片。。 然后我让 Agent 调用这个 skill 来生成一个测试图片，测试一下 skill 里面的脚本。"
ai_summary = ""
summary = "消息称，大晚上我手搓了一个 skill 。skill 很简单，调用 evolink 的 api 来生成图片。。 然后我让 Agent 调用这个 skill 来生成一个测试图片，测试一下 skill 里面的脚本。"
summary_zh = ""
tags = []
list_page = 110
+++

<!-- AUTO-GENERATED: news page -->

大晚上我手搓了一个 skill 。

skill 很简单，调用 evolink 的 api 来生成图片。

。

然后我让 Agent 调用这个 skill 来生成一个测试图片，测试一下 skill 里面的脚本。

我的原话是 “evolink skill 画个极简图片我看看”, 结果小米 MiMO 大模型给我生成了个日本国旗。

Agent 执行的关键日志如下, 下面是它生成的调用脚本完整的命令:
evolink skill 画个极简图片我看看
Thinking...
[skill] evolink (ctrl+o to expand)
Thinking...
$ python ~/.agents/skills/evolink/scripts/evolink_image.py generate \
--model gpt-image-2 \

## 🔗 原始来源

如果你要核对细节，可以再看原文：
[V2EX原文链接](https://www.v2ex.com/t/1221610)

